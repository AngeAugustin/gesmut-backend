# Database
# Pour développement local :
# MONGODB_URI=mongodb://127.0.0.1:27017/gesmut
# Pour MongoDB Atlas (en ligne) :
# MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/gesmut?retryWrites=true&w=majority
# Remplacez username, password et l'URL de votre cluster Atlas
MONGODB_URI=mongodb://127.0.0.1:27017/gesmut

# JWT
JWT_SECRET=your-secret-key-here-change-this-in-production
JWT_EXPIRES_IN=24h

# Server
PORT=3000
NODE_ENV=development

# CORS
# Pour développement local uniquement
CORS_ORIGIN=http://localhost:3001
# Pour accès depuis le réseau local, vous pouvez spécifier plusieurs origines séparées par des virgules
# Exemple: CORS_ORIGIN=http://localhost:3001,http://192.168.1.61:3001
# Ou laisser vide pour autoriser automatiquement les adresses IP locales (192.168.x.x, 10.x.x.x, 172.x.x.x)

# File Upload
MAX_FILE_SIZE=3145728
MAX_FILES_PER_REQUEST=4

# SMTP Configuration (Hostinger)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@celvox.org
SMTP_PASS=Celvox@2025
SMTP_FROM=noreply@celvox.org
SMTP_FROM_NAME=GESMUT
SMTP_TLS_REJECT_UNAUTHORIZED=false

